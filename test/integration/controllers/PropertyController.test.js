'use strict'
/* global describe, it */

const assert = require('assert')

describe('PropertyController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['PropertyController'])
  })
})
